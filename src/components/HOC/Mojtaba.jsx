import React from "react";

// const Toplearn = ({ children }) => {
//     return children;
// };

const Mojtaba = ({ children, classes }) => {
    return <div className={classes}>{children}</div>;
};

export default Mojtaba;
